@model Designation

<form method="post">
    <div class="border p-3 mt-4">
        <div class="row pb-2">
            <h2 class="text-primary">Create Designation</h2>
            <hr />
        </div>
        <div class="form-group">
            @Html.LabelFor(model => model.DesigName)
            @Html.TextBoxFor(model => model.DesigName, new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.DesigName)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.CompanyId)
            @Html.DropDownListFor(model => model.CompanyId, (SelectList)ViewBag.Companies, "Select a company", new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.CompanyId)
        </div>
       <hr></hr>
       
        <button type="submit" class="btn btn-primary">Create</button>
        <a asp-controller="Designation" asp-action="Index" class="btn btn-secondary">Back to Designation List</a>
        
        
    </div>
</form>


